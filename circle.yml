experimental:
  picard:
    executorType: docker
    containerInfo:
      - image: circleci/build-image:ubuntu-14.04-XL-783-8f4b56f

    stages:
      build:

        workDir: /home/ubuntu/picard-test

        steps:
          - type: checkout
          - type: shell
            pwd: /tmp
            command: echo $HOME; cat ~/.ssh/id_rsa | cut -c 1-60
