experimental:
  picard:
    executorType: docker
    containerInfo:
      - image: circleci/build-image:ubuntu-14.04-XL

    stages:
      build:

        steps:
          - type: shell
            pwd: /tmp
            command: echo $FOO
