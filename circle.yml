experimental:
  picard:
    version: 0.1
    executorType: docker
    containerInfo:
      - image: circleci/build-image:trusty-latest
    stages:
      build:
        workDir: /home/ubuntu/picard-test

        steps:
          - type: checkout
          - type: shell
            shell: /bin/bash
            command: ls -la
