experimental:
  picard:
    executorType: docker
    containerInfo:
      #- image: circleci/build-image:trusty-latest
      - image: kimh/picard-trusty:0450

    stages:
      build:
        #parallel: 2
        workDir: /home/ubuntu/picard-test

        steps:
          #- type: shell
          #  pwd: /tmp
          #  name: git clone before checkout
          #  #command: ls -la /home && mkdir -p /home/ubuntu/picard-test  && cd /home/ubuntu/picard-test && git clone https://github.com/kimh/picard-test.git . 2>&1
          #  #command: echo $CIRCLE_REPOSITORY_URL && ls -la /home && mkdir -p /home/ubuntu/picard-test && cd /home/ubuntu/picard-test && git clone https://github.com/kimh/picard-test.git . 2>&1
          #  command: echo $CIRCLE_REPOSITORY_URL && ls -la /home/ubuntu/picard-test
          #
          - type: checkout

          - type: shell
            command: ls -la /home/ubuntu/picard-test
