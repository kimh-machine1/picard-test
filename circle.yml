experimental:
  picard:
    executorType: docker
    containerInfo:
      - image: circleci/build-image:ubuntu-14.04-XL

    stages:
      build:

        steps:
          - type: shell
            shell: /bin/bash
            pwd: /tmp
            command: ruby -v

          - type: shell
            shell: /bin/bash
            pwd: /tmp
            command: source ~/.bash_profile && ruby -v
