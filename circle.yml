experimental:
  picard:
    executorType: docker
    containerInfo:
      - image: circleci/build-image:trusty-latest

    stages:
      build:

        steps:
          - type: shell
            pwd: /tmp
            command: sleep 30

