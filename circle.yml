experimental:
  picard:
    executorType: docker
    containerInfo:
      #- image: circleci/build-image:ubuntu-14.04-XL
      - image: circleci/build-image:trusty-latest
        user: ubuntu

    stages:
      build:

        steps:
          - type: shell
            shell: /bin/bash
            pwd: /tmp
            command: ruby -v

          - type: shell
            shell: /bin/bash
            pwd: /tmp
            command: source ~/.bash_profile && ruby -v
