experimental:
  picard:
    executorType: docker
    containerInfo:
      - image: kimh/picard-trusty:5740

    stages:
      build:

        steps:
          - type: checkout
          - type: add-ssh-keys
          - type: shell
            pwd: /tmp
            command: echo trump 

