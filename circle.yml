experimental:
  picard:
    executorType: docker
    containerInfo:
      #- image: circleci/build-image:ubuntu-14.04-XL
      - image: circleci/build-image:trusty-latest
      #- image: circleci/build-image:ubuntu-14.04-XL-781-14bf69a

    stages:
      build:

        steps:
          - type: shell
            shell: /bin/bash --login
            #pwd: /tmp
            command: ruby --version

          - type: shell
            shell: /bin/bash --login
            #pwd: /tmp
            command: pwd

          - type: shell
            shell: /bin/bash --login
            #pwd: /tmp
            command: printenv PATH

