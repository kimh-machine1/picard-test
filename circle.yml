experimental:
  picard:
    executorType: docker
    containerInfo:
      - image: kimh/picard-trusty:5740

    stages:
      build:
	workDir: /home/ubuntu/picard-test

        steps:
          - type: checkout
          - type: shell
            command: pwd; ls -la

