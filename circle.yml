experimental:
  picard:
    executorType: docker
    containerInfo:
      #- image: circleci/build-image:trusty-latest
      - image: kimh/picard-trusty:5740
      - image: golang:1.6.2

    stages:
      build:
        #parallel: 2
        workDir: /home/ubuntu/picard-test

        steps:
          - type: checkout

          - type: shell
            command: ls -la /home/ubuntu/picard-test

