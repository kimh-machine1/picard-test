experimental:
  picard:
    executorType: docker
    containerInfo:
      #- image: circleci/build-image:trusty-latest
      - image: kimh/picard-trusty:foo

    stages:
      build:
        #parallel: 2
        workDir: /home/ubuntu/picard-foo

        steps:
          #- type: shell
          #  shell: /bin/bash
          #  name: create picard-foo directory before checkout
          #  command: mkdir -p /home/ubuntu/picard-foo

          - type: checkout

          - type: shell
            name: create picard-foo directory before checkout
            command: ls -la && pwd


