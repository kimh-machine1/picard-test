experimental:
  picard:
    version: 0.1
    executorType: docker
    containerInfo:
      #- image: circleci/build-image:trusty-latest
      - image: kimh/picard-trusty:foo

    stages:
      build:
        #parallel: 2
        workDir: /home/ubuntu

        steps:
          - type: checkout
          - type: shell
            shell: /bin/bash
            command: ls -la
          - type: shell
            shell: /bin/bash
            command: pwd

