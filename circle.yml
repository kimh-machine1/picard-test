experimental:
  picard:
    version: 0.1
    executorType: docker
    containerInfo:
      #- image: circleci/build-image:trusty-latest
      #- image: kimh/picard-trusty:latest
      - image: ubuntu:14.04

    stages:
      build:
        #parallel: 2
        workDir: /home/ubuntu/picard-test

        steps:
          - type: checkout
          - type: shell
            shell: /bin/bash
            command: ls -la

