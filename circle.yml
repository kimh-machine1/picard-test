experimental:
  picard:
    executorType: docker
    containerInfo:
      #- image: circleci/build-image:ubuntu-14.04-XL
      - image: circleci/build-image:trusty-latest
        env:
          - BASH_ENV=/home/ubuntu/.bashrc
      #- image: circleci/build-image:ubuntu-14.04-XL-781-14bf69a

    stages:
      build:

        steps:
          - type: shell
            shell: /bin/bash 
            #pwd: /tmp
            command: printenv BASH_ENV
            env:
              - BASH_ENV=/home/ubuntu/.bashrc
 
          - type: shell
            shell: /bin/bash
            #pwd: /tmp
            command: pwd

          - type: shell
            shell: /bin/bash
            #pwd: /tmp
            command: printenv PATH

