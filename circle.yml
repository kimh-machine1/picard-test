experimental:
  picard:
    executorType: docker
    containerInfo:
      #- image: circleci/build-image:trusty-latest
      - image: kimh/picard-trusty:2551

    stages:
      build:
        #parallel: 2
        workDir: /home/ubuntu/picard-foo

        steps:
          - type: shell
            pwd: /tmp
            name: git clone before checkout
            command: git clone https://github.com/kimh/picard-test.git /home/ubuntu/picard-foo

          - type: checkout
